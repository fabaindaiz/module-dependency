from abc import ABC, abstractmethod
from dependency.core import Component, component
from {{ module.path }} import {{ module.name }}

class {{ component.interface }}(ABC):
    @abstractmethod
    def method(self) -> None:
        pass

@component(
    module={{ module.name }},
)
class {{ component.name }}({{ component.interface }}, Component):
    pass
